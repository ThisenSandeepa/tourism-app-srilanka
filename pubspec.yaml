name: ceylon_tours_pro
description: "Ceylon Tours Pro - Driver Assistant Platform for Sri Lanka Tourism"
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ^3.9.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  google_fonts: ^6.2.1
  provider: ^6.1.2
  shared_preferences: ^2.3.4
  # Uncomment these when Firebase project is configured:
  # firebase_core: ^3.12.1
  # firebase_auth: ^5.5.1
  # cloud_firestore: ^5.6.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
